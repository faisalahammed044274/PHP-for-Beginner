<?php

// Understand how to read the function library:

// https://www.php.net/manual/en/about.prototypes.php

// Read : str_replace()

// How to read functions from PHP Library.

/*

## We could rewrite the above function definition in a generic way:

    function name    ( parameter type   parameter name ) : returned type

## Many functions take on multiple parameters, such as in_array().
Its prototype is as follows:

    in_array ( mixed $needle, array $haystack , bool $strict = false ) : bool

*/


// str_replace(
//     array|string $search,
//     array|string $replace,
//     string|array $subject,
//     int &$count = null
// ): string|array

//Need Revision